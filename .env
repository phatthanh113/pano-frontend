# Dev: Vite proxy sẽ forward /api và /storage tới Laravel nên để rỗng = same-origin
VITE_API_BASE_URL=
# Production: khi build vào Laravel public cũng same-origin, không cần set
# Nếu deploy tách domain thì set: VITE_API_BASE_URL=https://api.yourdomain.com
